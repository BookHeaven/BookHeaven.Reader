<header class="relative z-10 flex items-center h-[40px] border-y border-b-black border-t-neutral-300 overflow-hidden">
    @ChildContent
</header>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}