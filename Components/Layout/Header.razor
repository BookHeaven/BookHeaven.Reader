<header class="relative z-10 flex items-center h-[40px] border-black border-b overflow-hidden">
    @ChildContent
</header>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}